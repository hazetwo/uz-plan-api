# better-uz-plan

Go API for scraping and caching UZ schedule data.

## Commands

```bash
make run          # run the server
make test         # run tests
make build        # build binary to ./bin/app
make start-redis  # start Redis via Docker Compose
```

## Docker

```bash
docker compose up
```
